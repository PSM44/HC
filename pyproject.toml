[project]
name = "hc-control"
version = "0.1.0"
description = "Harness Council local control-plane experiments"
requires-python = ">=3.12,<3.14"
dependencies = [
  "mcp[cli]==2.2.0",
]

[tool.uv]
package = false
